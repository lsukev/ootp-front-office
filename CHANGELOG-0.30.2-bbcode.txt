[b][size=4]OOTP Front Office 0.30.2 — everything since 0.30.1[/size][/b]
[list]
[*][b]Fixed[/b] — Bullpen availability was a day behind for every pitcher. Where it said today it meant yesterday, yesterday meant two days ago, and two days rested meant three. The page asked the games table for the newest date it held and called that today — but you export after simming, so the newest games in the file are the ones just played and the league has already moved on to the next morning, which is the only reason there is a game tonight to be available for. The availability itself and the pitch counts were right throughout; only the naming of the day was wrong. It reads the league's own date now. The rotation's rest column and the projected next start counted from the same day and were short by one in the same way.
[*][b]Fixed[/b] — The column tooltips are drawn over the table instead of under it. They looked semi-transparent, with the rating bars from the rows beneath showing through. They were never transparent: a sticky table header sits in its own stacking layer, and without a level of its own it landed beneath the cells in the body, which are drawn later. The same omission let those bars slide over the header itself on a scrolled table.
[/list]
